# 生活经验分享平台 (Lifestyle Experience Sharing Platform)

一个类似于 GitHub 的分享网站，专注于生活经验、技巧和知识的分享。用户可以发现各类生活技巧，也可以分享自己的经验。平台设计支持向移动应用转换。

## 功能特性

### 核心功能
- 📝 **发布与管理**：创建、编辑、删除生活经验文章
- 🏷️ **分类与标签**：通过分类和标签组织内容
- 🔍 **搜索与筛选**：快速找到感兴趣的内容
- 💬 **评论系统**：在文章下进行讨论和交流
- ❤️ **点赞收藏**：点赞喜欢的内容并收藏以便后续查看

### GitHub 风格特性
- ✏️ **Markdown 支持**：使用 Markdown 格式编写内容
- 📜 **编辑历史**：查看文章的修改历史
- 🔀 **转载功能**：类似 Fork，可以转载他人的经验
- ⭐ **收藏系统**：类似 Star，收藏优质内容
- 👥 **关注系统**：关注感兴趣的用户
- 📊 **活动统计**：查看浏览量、点赞数等数据

### 技术特性
- 🔐 **用户认证**：JWT 基于令牌的安全认证
- 📱 **响应式设计**：适配各种设备屏幕
- 🚀 **RESTful API**：为未来移动应用做准备
- 🎨 **现代化界面**：清爽美观的用户界面

## 技术栈

### 后端
- **Node.js** + **Express.js**：服务器框架
- **MongoDB** + **Mongoose**：数据库
- **JWT**：用户认证
- **bcryptjs**：密码加密

### 前端
- **React**：用户界面框架
- **React Router**：路由管理
- **Zustand**：状态管理
- **Axios**：HTTP 客户端
- **React Markdown**：Markdown 渲染
- **Vite**：构建工具

## 快速开始

### 前置要求
- Node.js >= 14.0.0
- MongoDB >= 4.0

### 安装步骤

1. **克隆仓库**
```bash
git clone <repository-url>
cd -
```

2. **安装后端依赖**
```bash
cd backend
npm install
```

3. **配置后端环境变量**
```bash
cp .env.example .env
# 编辑 .env 文件，设置数据库连接和 JWT 密钥
```

4. **启动后端服务**
```bash
npm start
# 或开发模式
npm run dev
```

5. **安装前端依赖**
```bash
cd ../frontend
npm install
```

6. **配置前端环境变量**
```bash
cp .env.example .env
# 默认配置已经可以使用
```

7. **启动前端开发服务器**
```bash
npm run dev
```

访问 `http://localhost:5173` 查看应用。

## 项目结构

```
├── backend/                 # 后端代码
│   ├── config/             # 配置文件
│   ├── models/             # 数据模型
│   ├── routes/             # API 路由
│   ├── middleware/         # 中间件
│   └── server.js           # 服务器入口
│
├── frontend/               # 前端代码
│   ├── src/
│   │   ├── components/    # React 组件
│   │   ├── pages/         # 页面组件
│   │   ├── services/      # API 服务
│   │   ├── store/         # 状态管理
│   │   └── App.jsx        # 主应用组件
│   └── package.json
│
└── README.md
```

## API 文档

### 认证相关
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录

### 用户相关
- `GET /api/users/me` - 获取当前用户信息
- `GET /api/users/:id` - 获取指定用户信息
- `PUT /api/users/me` - 更新用户资料
- `POST /api/users/:id/follow` - 关注用户
- `POST /api/users/:id/unfollow` - 取消关注

### 文章相关
- `GET /api/posts` - 获取文章列表（支持分页、搜索、筛选）
- `GET /api/posts/:id` - 获取文章详情
- `POST /api/posts` - 创建文章
- `PUT /api/posts/:id` - 更新文章
- `DELETE /api/posts/:id` - 删除文章
- `POST /api/posts/:id/like` - 点赞/取消点赞
- `POST /api/posts/:id/fork` - 转载文章
- `POST /api/posts/:id/save` - 收藏/取消收藏

### 分类相关
- `GET /api/categories` - 获取所有分类
- `POST /api/categories` - 创建分类

### 评论相关
- `GET /api/comments/post/:postId` - 获取文章评论
- `POST /api/comments` - 发表评论
- `DELETE /api/comments/:id` - 删除评论
- `POST /api/comments/:id/like` - 点赞评论

## 使用指南

### 注册和登录
1. 访问首页，点击"注册"按钮
2. 填写用户名、邮箱和密码
3. 注册成功后自动登录

### 发布经验
1. 登录后，点击导航栏的"发布经验"
2. 填写标题、选择分类、添加标签
3. 使用 Markdown 格式编写内容
4. 点击"发布"按钮

### 浏览和搜索
1. 在首页可以看到所有文章
2. 使用搜索框搜索关键词
3. 点击分类按钮筛选特定类型的内容
4. 点击文章卡片查看详情

### 互动功能
- 点击 ❤️ 图标为文章点赞
- 点击 ⭐ 按钮收藏文章
- 点击 🔀 按钮转载文章
- 在文章详情页发表评论

## 未来计划

- [ ] 移动应用开发（React Native / Flutter）
- [ ] 图片上传功能
- [ ] 实时通知系统
- [ ] 用户积分和等级系统
- [ ] 内容推荐算法
- [ ] 社区管理功能
- [ ] 数据分析仪表板
- [ ] 多语言支持

## 贡献指南

欢迎贡献代码、报告问题或提出建议！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

ISC License

## 联系方式

如有问题或建议，欢迎通过 Issues 提出。

